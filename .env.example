# Database Configuration
# PostgreSQL connection string from Railway
DATABASE_URL=postgresql://user:password@host:port/database

# Example Railway format:
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@autorack.proxy.rlwy.net:27586/railway

# Environment
NODE_ENV=development

# Server Port (optional, defaults to 3000)
PORT=3000

# Note: For production deployment on Railway, these variables are automatically set
# For local development:
# 1. Copy this file to .env
# 2. Replace DATABASE_URL with your actual Railway database URL
# 3. You can find your database URL in the Railway dashboard under your PostgreSQL service
